# Frontend Quick Start (copy)

Copy of `FrontEnd/QUICK_START.md`.

## Install & Run
```bash
cd FrontEnd
npm install
npm run dev
```

## Environment
Create `.env.local` with API URLs (see original README).
